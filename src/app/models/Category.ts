export class Category {
    categoryId: number;
    category: string;
    constructor(categoryId:number, category:string) {
        this.categoryId = categoryId;
        this.category = category;
    }
}